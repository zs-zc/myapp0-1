const getters = {
    sidebar: state => state.app.sidebar,
    device: state => state.app.device,
    token: state => state.user.device,
    name: state => state.user.device,
    avatar: state => state.user.device,
}
export default getters